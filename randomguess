 import random 
  n = random.randint(1,100)
  a =-1
  guesses =0
  while (a!=n):
    guesses += 1
    a=int(input("enter your guess  [number]"))
    if(a>n):
      print("lower ")
    else:
      print("higher")
  
  print(f"u r correct the number is {n} guessed in {guesses} guesses ")
